import java.util.List;
import java.util.Set;

/**
 * Created by ilan on 11/21/16.
 */
public class Permutator {


    /**
     * Returns a set of all possible permutations so that list[i] < list[j] for size n
     * that is, if a list contains [1,2,3] and n = 2 then the output will be:
     * [[1,2], [1,3], [2,3]]
     * @param list
     * @param n
     * @param <T>
     * @return
     */
    private static <T> Set<T> permute(List<T> list, int n) {
        return null;
    }

    /**
     * Returns a set of all possible permutations on the list so that list[i] < list[j]
     * That is, if a list contains [1, 2, 3] the output will be
     * [[1], [2], [3], [1,2], [1,3], [2,3], [1,2,3]]
     * @param list
     * @param n
     * @param <T>
     * @return
     */
    public static <T> Set<T> permute(List<T> list) {
        return null;
    }

}
